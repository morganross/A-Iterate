# A-Iterate
AI API writes code. 

A-Iterate executes the code.

A-Iterate returns the output to AI API.

AI API revises code.

A-Iterate executes the code.

A-Iterate returns the output to AI API.

AI API revises code.

A-Iterate executes the code.

A-Iterate returns the output to AI API.
